package templates

import "github.com/Joao-Felisberto/devprivops-dashboard/data"
import "fmt"

templ RegulationsPage(report *data.Report) {
    <h1 class="heading text-3xl">
        Report for {report.Project}
    </h1>

    for _, regulation := range report.Regulations {
        @Regulation(report.Project, regulation)
    }
}

templ Regulation (project string, regulation *data.Regulation) {
    <div class="card">
        <a href={templ.URL(fmt.Sprintf("/%s/%s", project, regulation.Name))}>
            <h3 class="text-xl text-slate-300">
                {regulation.Name}
            </h3>
        </a>
    </div>
}